<UserControl
    x:Class="GetStoreApp.UI.Controls.Settings.RegionControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:gc="using:GetStoreApp.UI.Controls.General"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModel="using:GetStoreApp.ViewModels.Controls.Settings"
    mc:Ignorable="d">

    <UserControl.DataContext>
        <viewModel:RegionViewModel x:Name="ViewModel" />
    </UserControl.DataContext>

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/_SettingsFontIcon.xaml" />
                <ResourceDictionary Source="/Styles/_Thickness.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <gc:CardControl>
        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <FontIcon
                Grid.Column="0"
                Glyph="&#xF738;"
                Style="{StaticResource SettingsFontIconStyle}" />

            <StackPanel Grid.Column="1" Orientation="Vertical">
                <TextBlock
                    x:Uid="Settings_Region"
                    VerticalAlignment="Center"
                    Style="{StaticResource BodyTextBlockStyle}"
                    Text="The app is in the corresponding area of the Store"
                    TextWrapping="WrapWholeWords" />
                <TextBlock
                    x:Uid="Settings_Region_Description"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Style="{StaticResource CaptionTextBlockStyle}"
                    Text="If the app is available in a specific region, the region needs to be adjusted"
                    TextWrapping="WrapWholeWords" />
            </StackPanel>
            <ComboBox
                Grid.Column="2"
                DisplayMemberPath="FriendlyName"
                ItemsSource="{x:Bind ViewModel.RegionList}"
                SelectedValue="{x:Bind ViewModel.SelectedRegion, Mode=TwoWay}"
                SelectedValuePath="ISO2" />
        </Grid>
    </gc:CardControl>
</UserControl>
