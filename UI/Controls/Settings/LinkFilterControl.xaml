<UserControl
    x:Class="GetStoreApp.UI.Controls.Settings.LinkFilterControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:congen="using:GetStoreApp.Converters.General"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModel="using:GetStoreApp.ViewModels.Pages"
    mc:Ignorable="d">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/SettingsFontIcon.xaml" />
                <ResourceDictionary Source="/Styles/Thickness.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <congen:EnumToBooleanConverter x:Key="EnumToBooleanConverter" />

            <Style x:Key="SettingsExpanderStyle" TargetType="Expander">
                <Setter Property="ExpandDirection" Value="Down" />
                <Setter Property="IsExpanded" Value="False" />
                <Setter Property="HorizontalAlignment" Value="Stretch" />
                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                <Setter Property="VerticalAlignment" Value="Top" />
                <Setter Property="VerticalContentAlignment" Value="Stretch" />
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <!--  文件过滤设置  -->
    <Expander Style="{StaticResource SettingsExpanderStyle}">
        <Expander.Header>
            <Grid HorizontalAlignment="Stretch" VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <FontIcon
                    FontFamily="Segoe Fluent Icons"
                    Glyph="&#xE71c;"
                    Style="{StaticResource SettingsFontIconStyle}" />

                <StackPanel
                    Grid.Column="1"
                    Margin="0,12"
                    Orientation="Vertical">
                    <TextBlock Style="{StaticResource BodyTextBlockStyle}" Text="{x:Bind viewModel:SettingsViewModel.LinkFilter}" />
                    <TextBlock
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Style="{StaticResource CaptionTextBlockStyle}"
                        Text="{x:Bind viewModel:SettingsViewModel.LinkFilterDescription}"
                        TextWrapping="WrapWholeWords" />
                </StackPanel>

                <Button Grid.Column="2" Content="{x:Bind viewModel:SettingsViewModel.LinkFilterInstruction}">
                    <Button.Flyout>
                        <Flyout>
                            <StackPanel Width="350">
                                <Grid Margin="0,0,0,12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="75" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock
                                        Grid.Column="0"
                                        FontSize="13"
                                        Style="{ThemeResource CaptionTextBlockStyle}"
                                        Text="{x:Bind viewModel:SettingsViewModel.StartsWithEInstruction}"
                                        TextWrapping="Wrap" />
                                    <Button
                                        Grid.Column="1"
                                        HorizontalAlignment="Right"
                                        VerticalAlignment="Center"
                                        Command="{x:Bind ViewModel.StartsWithECommand}"
                                        Content="{x:Bind viewModel:SettingsViewModel.StartsWithEButton}" />
                                </Grid>

                                <MenuFlyoutSeparator />

                                <Grid Margin="0,12,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="75" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock
                                        Grid.Column="0"
                                        FontSize="13"
                                        Style="{ThemeResource CaptionTextBlockStyle}"
                                        Text="{x:Bind viewModel:SettingsViewModel.BlockMapInstruction}"
                                        TextWrapping="Wrap" />
                                    <Button
                                        Grid.Column="1"
                                        HorizontalAlignment="Right"
                                        VerticalAlignment="Center"
                                        Command="{x:Bind ViewModel.BlockMapCommand}"
                                        Content="{x:Bind viewModel:SettingsViewModel.BlockMapButton}" />
                                </Grid>
                            </StackPanel>
                        </Flyout>
                    </Button.Flyout>
                </Button>
            </Grid>
        </Expander.Header>

        <Expander.Content>
            <StackPanel>
                <StackPanel Margin="26,-9,0,5">
                    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Center">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Vertical">
                            <TextBlock
                                VerticalAlignment="Center"
                                Style="{StaticResource BodyTextBlockStyle}"
                                Text="{x:Bind viewModel:SettingsViewModel.StartsWithE}"
                                TextWrapping="WrapWholeWords" />
                            <TextBlock
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Style="{StaticResource CaptionTextBlockStyle}"
                                Text="{x:Bind viewModel:SettingsViewModel.StartsWithEDescription}"
                                TextWrapping="WrapWholeWords" />
                        </StackPanel>
                        <ToggleSwitch
                            Grid.Column="1"
                            FlowDirection="RightToLeft"
                            IsOn="{x:Bind ViewModel.StartsWithEFilterValue, Mode=TwoWay}"
                            OffContent="{x:Bind viewModel:SettingsViewModel.StartsWithEOffContent}"
                            OnContent="{x:Bind viewModel:SettingsViewModel.StartsWithEOnContent}" />
                    </Grid>
                </StackPanel>

                <MenuFlyoutSeparator />

                <StackPanel Margin="26,5,0,-9">
                    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Center">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Vertical">
                            <TextBlock
                                VerticalAlignment="Center"
                                Style="{StaticResource BodyTextBlockStyle}"
                                Text="{x:Bind viewModel:SettingsViewModel.BlockMap}"
                                TextWrapping="WrapWholeWords" />
                            <TextBlock
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Style="{StaticResource CaptionTextBlockStyle}"
                                Text="{x:Bind viewModel:SettingsViewModel.BlockMapDescription}"
                                TextWrapping="WrapWholeWords" />
                        </StackPanel>
                        <ToggleSwitch
                            Grid.Column="1"
                            FlowDirection="RightToLeft"
                            IsOn="{x:Bind ViewModel.BlockMapFilterValue, Mode=TwoWay}"
                            OffContent="{x:Bind viewModel:SettingsViewModel.BlockMapOffContent}"
                            OnContent="{x:Bind viewModel:SettingsViewModel.BlockMapOnContent}" />
                    </Grid>
                </StackPanel>
            </StackPanel>
        </Expander.Content>
    </Expander>
</UserControl>