<UserControl
    x:Class="GetStoreApp.UI.Controls.Settings.LinkFilterControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:service="using:GetStoreApp.Services.Settings"
    mc:Ignorable="d">

    <Expander Style="{StaticResource DefaultExpanderStyle}">
        <Expander.Header>
            <Grid HorizontalAlignment="Stretch" VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <FontIcon
                    FontFamily="Segoe Fluent Icons"
                    Glyph="&#xE71c;"
                    Style="{StaticResource FontIconStyle}" />

                <StackPanel
                    Grid.Column="1"
                    Margin="0,12"
                    Orientation="Vertical">

                    <TextBlock
                        VerticalAlignment="Center"
                        Style="{StaticResource BodyTextBlockStyle}"
                        Text="{x:Bind service:LanguageService.GetResources('/Settings/LinkFilter')}" />

                    <TextBlock
                        VerticalAlignment="Center"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Style="{StaticResource CaptionTextBlockStyle}"
                        Text="{x:Bind service:LanguageService.GetResources('/Settings/LinkFilterDescription')}" />
                </StackPanel>

                <Button Grid.Column="2" Content="{x:Bind service:LanguageService.GetResources('/Settings/LinkFilterInstruction')}">
                    <Button.Flyout>
                        <Flyout>
                            <StackPanel Width="350">
                                <Grid Margin="0,0,0,12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="75" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock
                                        Grid.Column="0"
                                        FontSize="13"
                                        Style="{ThemeResource CaptionTextBlockStyle}"
                                        Text="{x:Bind service:LanguageService.GetResources('/Settings/StartsWithEInstruction')}" />
                                    <Button
                                        Grid.Column="1"
                                        HorizontalAlignment="Right"
                                        VerticalAlignment="Center"
                                        Command="{x:Bind ViewModel.StartsWithECommand}"
                                        Content="{x:Bind service:LanguageService.GetResources('/Settings/StartsWithEButton')}" />
                                </Grid>

                                <MenuFlyoutSeparator />

                                <Grid Margin="0,12,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="75" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock
                                        Grid.Column="0"
                                        FontSize="13"
                                        Style="{ThemeResource CaptionTextBlockStyle}"
                                        Text="{x:Bind service:LanguageService.GetResources('/Settings/BlockMapInstruction')}" />
                                    <Button
                                        Grid.Column="1"
                                        HorizontalAlignment="Right"
                                        VerticalAlignment="Center"
                                        Command="{x:Bind ViewModel.BlockMapCommand}"
                                        Content="{x:Bind service:LanguageService.GetResources('/Settings/BlockMapButton')}" />
                                </Grid>
                            </StackPanel>
                        </Flyout>
                    </Button.Flyout>
                </Button>
            </Grid>
        </Expander.Header>

        <Expander.Content>
            <StackPanel Padding="43,0,18,0">
                <StackPanel Margin="0,6,0,6">
                    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Center">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Vertical">
                            <TextBlock
                                VerticalAlignment="Center"
                                Style="{StaticResource BodyTextBlockStyle}"
                                Text="{x:Bind service:LanguageService.GetResources('/Settings/StartsWithE')}" />
                            <TextBlock
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Style="{StaticResource CaptionTextBlockStyle}"
                                Text="{x:Bind service:LanguageService.GetResources('/Settings/StartsWithEDescription')}" />
                        </StackPanel>

                        <ToggleSwitch
                            Grid.Column="1"
                            FlowDirection="RightToLeft"
                            IsOn="{x:Bind ViewModel.StartsWithEFilterValue, Mode=TwoWay}"
                            OffContent="{x:Bind service:LanguageService.GetResources('/Settings/StartsWithEOffContent')}"
                            OnContent="{x:Bind service:LanguageService.GetResources('/Settings/StartsWithEOnContent')}" />
                    </Grid>
                </StackPanel>

                <MenuFlyoutSeparator />

                <StackPanel Margin="0,6,0,6">
                    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Center">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Vertical">
                            <TextBlock
                                VerticalAlignment="Center"
                                Style="{StaticResource BodyTextBlockStyle}"
                                Text="{x:Bind service:LanguageService.GetResources('/Settings/BlockMap')}" />
                            <TextBlock
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Style="{StaticResource CaptionTextBlockStyle}"
                                Text="{x:Bind service:LanguageService.GetResources('/Settings/BlockMapDescription')}" />
                        </StackPanel>

                        <ToggleSwitch
                            Grid.Column="1"
                            FlowDirection="RightToLeft"
                            IsOn="{x:Bind ViewModel.BlockMapFilterValue, Mode=TwoWay}"
                            OffContent="{x:Bind service:LanguageService.GetResources('/Settings/BlockMapOffContent')}"
                            OnContent="{x:Bind service:LanguageService.GetResources('/Settings/BlockMapOnContent')}" />
                    </Grid>
                </StackPanel>
            </StackPanel>
        </Expander.Content>
    </Expander>
</UserControl>
