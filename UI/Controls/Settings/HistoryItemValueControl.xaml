<UserControl
    x:Class="GetStoreApp.UI.Controls.Settings.HistoryItemValueControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:gc="using:GetStoreApp.UI.Controls.General"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModel="using:GetStoreApp.ViewModels.Controls.Settings"
    mc:Ignorable="d">

    <UserControl.DataContext>
        <viewModel:HistoryItemValueViewModel x:Name="ViewModel" />
    </UserControl.DataContext>

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/_SettingsFontIcon.xaml" />
                <ResourceDictionary Source="/Styles/_Thickness.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <gc:CardControl>
        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <FontIcon
                Grid.Column="0"
                Glyph="&#xF738;"
                Style="{StaticResource SettingsFontIconStyle}" />

            <StackPanel Grid.Column="1" Orientation="Vertical">
                <TextBlock
                    x:Uid="Settings_HistoryItem"
                    VerticalAlignment="Center"
                    Style="{StaticResource BodyTextBlockStyle}"
                    Text="The number of main page history item displays"
                    TextWrapping="WrapWholeWords" />
                <TextBlock
                    x:Uid="Settings_HistoryItem_Description"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Style="{StaticResource CaptionTextBlockStyle}"
                    Text="The maximum number of history entries that will appear when the main page opens after the app launches"
                    TextWrapping="WrapWholeWords" />
            </StackPanel>
            <ComboBox
                Grid.Column="2"
                DisplayMemberPath="SimpleHisItemName"
                ItemsSource="{x:Bind ViewModel.SimpleHisItemList}"
                SelectedValue="{x:Bind ViewModel.SelectedHisItemValue, Mode=TwoWay}"
                SelectedValuePath="SimpleHisItemValue" />
        </Grid>
    </gc:CardControl>
</UserControl>
