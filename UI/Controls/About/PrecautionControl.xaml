<UserControl
    x:Class="GetStoreApp.UI.Controls.About.PrecautionControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Expander Style="{StaticResource ExpanderAboutStyle}">
        <Expander.Header>
            <TextBlock Style="{StaticResource ExpanderHeaderStyle}" Text="{x:Bind ResourceService.GetLocalized('/About/Precaution')}" />
        </Expander.Header>

        <Expander.Content>
            <StackPanel Margin="14,0,10,0">

                <TextBlock Style="{StaticResource ExpanderContentTitleStyle}" Text="{x:Bind ResourceService.GetLocalized('/About/PaidApps')}" />
                <TextBlock Style="{StaticResource ExpanderContentDescriptionStyle}" Text="{x:Bind ResourceService.GetLocalized('/About/PaidAppsDescription')}" />
                <MenuFlyoutSeparator Margin="0,0,0,8" />

                <TextBlock Style="{StaticResource ExpanderContentTitleStyle}" Text="{x:Bind ResourceService.GetLocalized('/About/DesktopApps')}" />
                <TextBlock Style="{StaticResource ExpanderContentDescriptionStyle}" Text="{x:Bind ResourceService.GetLocalized('/About/DesktopAppsDescription')}" />

                <HyperlinkButton
                    Margin="-5,0,0,0"
                    Padding="5"
                    Command="{x:Bind ViewModel.RecognizeCommand}"
                    Content="{x:Bind ResourceService.GetLocalized('/About/RecognizeDesktopApps')}"
                    ToolTipService.ToolTip="{x:Bind ResourceService.GetLocalized('/About/RecognizeDesktopApps')}" />
            </StackPanel>
        </Expander.Content>
    </Expander>
</UserControl>
