<UserControl
    x:Class="GetStoreApp.UI.Controls.General.CardControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:general="using:GetStoreApp.ViewModels.Controls.General"
    d:DataContext="{d:DesignInstance Type=general:CardViewModel}"
    mc:Ignorable="d">

    <Grid
        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
        BorderThickness="1"
        CornerRadius="{ThemeResource ControlCornerRadius}">
        <Grid Padding="16,12" Background="Transparent">
            <ContentPresenter Content="{x:Bind HomeContent}" />
        </Grid>
    </Grid>
</UserControl>
