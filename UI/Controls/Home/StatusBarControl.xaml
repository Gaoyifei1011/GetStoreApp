<UserControl
    x:Class="GetStoreApp.UI.Controls.Home.StatusBarControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/Converter.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <StackPanel>
        <InfoBar
            IsClosable="False"
            IsOpen="True"
            Message="{x:Bind ViewModel.StateInfoText, Mode=OneWay}"
            Severity="{x:Bind ViewModel.InfoBarSeverity, Mode=OneWay}">
            <InfoBar.Content>
                <ProgressBar
                    Margin="-45,0,0,0"
                    IsIndeterminate="{x:Bind ViewModel.StatePrBarActValue, Mode=OneWay}"
                    Visibility="{x:Bind ViewModel.StatePrBarVisValue, Converter={StaticResource BooleanToVisibilityConverter}, Mode=OneWay}" />
            </InfoBar.Content>
        </InfoBar>
    </StackPanel>
</UserControl>
