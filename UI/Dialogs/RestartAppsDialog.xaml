<ContentDialog
    x:Class="GetStoreApp.UI.Dialogs.RestartAppsDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="{x:Bind ResourceService.GetLocalized('/Dialog/Restart')}"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    CloseButtonText="{x:Bind ResourceService.GetLocalized('/Dialog/RestartCancel')}"
    Content="{x:Bind ResourceService.GetLocalized('/Dialog/RestartContent')}"
    PrimaryButtonStyle="{ThemeResource AccentButtonStyle}"
    PrimaryButtonText="{x:Bind ResourceService.GetLocalized('/Dialog/RestartSure')}"
    Style="{ThemeResource DefaultContentDialogStyle}"
    mc:Ignorable="d" />
