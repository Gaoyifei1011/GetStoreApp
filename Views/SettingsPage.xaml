<Page
    x:Class="GetStoreApp.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:service="using:GetStoreApp.Services.Settings"
    xmlns:sc="using:GetStoreApp.UI.Controls.Settings"
    NavigationCacheMode="Required"
    mc:Ignorable="d">

    <ScrollViewer HorizontalScrollMode="Disabled" VerticalScrollBarVisibility="Auto">
        <StackPanel
            Margin="30,15,30,0"
            Padding="0,0,0,40"
            Spacing="10">

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock
                    Grid.Column="0"
                    FontSize="{StaticResource TitleFontSize}"
                    Text="{x:Bind service:LanguageService.GetResources('/Settings/Title')}" />

                <Button
                    Grid.Column="1"
                    Command="{x:Bind ViewModel.RestartCommand}"
                    Content="{x:Bind service:LanguageService.GetResources('/Settings/RestartApp')}" />
            </Grid>

            <TextBlock
                FontSize="16"
                Style="{StaticResource BodyTextBlockStyle}"
                Text="{x:Bind service:LanguageService.GetResources('/Settings/Appearance')}" />

            <sc:ThemeControl />

            <sc:BackdropControl />

            <sc:LauguageControl />

            <TextBlock
                FontSize="16"
                Style="{StaticResource BodyTextBlockStyle}"
                Text="{x:Bind service:LanguageService.GetResources('/Settings/General')}" />

            <sc:UseInstructionControl />

            <sc:HistoryItemValueControl />

            <sc:RegionControl />

            <sc:LinkFilterControl />

            <TextBlock
                FontSize="16"
                Style="{StaticResource BodyTextBlockStyle}"
                Text="{x:Bind service:LanguageService.GetResources('/Settings/Advanced')}" />

            <sc:ClearRecordControl />
        </StackPanel>
    </ScrollViewer>
</Page>
