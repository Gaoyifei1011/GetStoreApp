<Page
    x:Class="GetStoreApp.Views.AboutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ac="using:GetStoreApp.UI.Controls.About"
    mc:Ignorable="d">

    <ScrollViewer HorizontalScrollMode="Disabled" VerticalScrollBarVisibility="Auto">
        <StackPanel
            Margin="30,15,30,0"
            Padding="0,0,0,40"
            Spacing="10">

            <TextBlock FontSize="{StaticResource TitleFontSize}" Text="{x:Bind ResourceService.GetLocalized('/About/Title')}" />

            <ac:HeaderControl />

            <ac:IntroductionControl />

            <ac:ReferenceControl />

            <ac:InstructionsControl />

            <ac:PrecautionControl />

            <ac:SettingsHelpControl />
        </StackPanel>
    </ScrollViewer>
</Page>
