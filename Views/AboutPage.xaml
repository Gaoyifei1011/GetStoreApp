<Page
    x:Class="GetStoreApp.Views.AboutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ac="using:GetStoreApp.UI.Controls.About"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:service="using:GetStoreApp.Services.Settings"
    mc:Ignorable="d">

    <ScrollViewer HorizontalScrollMode="Disabled" VerticalScrollBarVisibility="Auto">
        <StackPanel
            Margin="30,15,30,0"
            Padding="0,0,0,40"
            Spacing="10">

            <TextBlock
                Grid.ColumnSpan="2"
                FontSize="{StaticResource TitleFontSize}"
                Text="{x:Bind service:LanguageService.GetResources('/About/Title')}" />

            <ac:HeaderControl />

            <ac:IntroductionControl />

            <ac:ReferenceControl />

            <ac:InstructionsControl />

            <ac:PrecautionControl />

            <ac:ThanksControl />
        </StackPanel>
    </ScrollViewer>
</Page>
