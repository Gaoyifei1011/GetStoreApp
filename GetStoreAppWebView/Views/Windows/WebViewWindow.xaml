<Window
    x:Class="GetStoreAppWebView.Views.Windows.WebViewWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:backdrophelper="using:GetStoreAppWebView.Helpers.Backdrop"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:rootservice="using:GetStoreAppWebView.Services.Root"
    xmlns:settingsservice="using:GetStoreAppWebView.Services.Settings"
    Title="{x:Bind WindowTitle, Mode=OneWay}"
    Activated="{x:Bind OnActivated}"
    SizeChanged="{x:Bind OnSizeChanged}"
    SystemBackdrop="{x:Bind WindowSystemBackdrop, Mode=OneWay}"
    mc:Ignorable="d">

    <Page
        x:Name="WebViewPage"
        ActualThemeChanged="{x:Bind OnActualThemeChanged}"
        FlowDirection="{x:Bind settingsservice:LanguageService.FlowDirection}"
        Loaded="{x:Bind OnLoaded}"
        RequestedTheme="{x:Bind WindowTheme, Mode=OneWay}">

        <Page.Resources>
            <ResourceDictionary>
                <ResourceDictionary.ThemeDictionaries>
                    <ResourceDictionary x:Key="Default">
                        <Color x:Key="DefaultBackgroundColor">#141414</Color>
                        <SolidColorBrush x:Key="DefaultBackgroundColorBrush" Color="{ThemeResource DefaultBackgroundColor}" />
                    </ResourceDictionary>
                    <ResourceDictionary x:Key="HighConstract">
                        <Color x:Key="DefaultBackgroundColor">#FF0000</Color>
                        <SolidColorBrush x:Key="DefaultBackgroundColorBrush" Color="{ThemeResource DefaultBackgroundColor}" />
                    </ResourceDictionary>
                    <ResourceDictionary x:Key="Light">
                        <Color x:Key="DefaultBackgroundColor">#F0F3F9</Color>
                        <SolidColorBrush x:Key="DefaultBackgroundColorBrush" Color="{ThemeResource DefaultBackgroundColor}" />
                    </ResourceDictionary>
                </ResourceDictionary.ThemeDictionaries>

                <!--  标题栏右键菜单  -->
                <MenuFlyout
                    x:Name="TitlebarMenuFlyout"
                    x:FieldModifier="public"
                    Placement="BottomEdgeAlignedLeft">

                    <MenuFlyoutItem
                        Height="28"
                        Padding="11,0"
                        Click="{x:Bind OnRestoreClicked}"
                        FontSize="12"
                        IsEnabled="{x:Bind IsWindowMaximized, Mode=OneWay}"
                        Text="{x:Bind rootservice:ResourceService.GetLocalized('Installer/Restore')}">

                        <MenuFlyoutItem.Icon>
                            <FontIcon Margin="6" Glyph="&#xE923;" />
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>

                    <MenuFlyoutItem
                        Height="28"
                        Padding="11,0"
                        Click="{x:Bind OnMoveClicked}"
                        FontSize="12"
                        IsEnabled="{x:Bind IsWindowMaximized.Equals(x:False), Mode=OneWay}"
                        Tag="{x:Bind TitlebarMenuFlyout}"
                        Text="{x:Bind rootservice:ResourceService.GetLocalized('Installer/Move')}" />

                    <MenuFlyoutItem
                        Height="28"
                        Padding="11,0"
                        Click="{x:Bind OnSizeClicked}"
                        FontSize="12"
                        IsEnabled="{x:Bind IsWindowMaximized.Equals(x:False), Mode=OneWay}"
                        Tag="{x:Bind TitlebarMenuFlyout}"
                        Text="{x:Bind rootservice:ResourceService.GetLocalized('Installer/Size')}" />

                    <MenuFlyoutItem
                        Height="28"
                        Padding="11,0"
                        Click="{x:Bind OnMinimizeClicked}"
                        FontSize="12"
                        Text="{x:Bind rootservice:ResourceService.GetLocalized('Installer/Minimize')}">
                        <MenuFlyoutItem.Icon>
                            <FontIcon Margin="6" Glyph="&#xE921;" />
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>

                    <MenuFlyoutItem
                        Height="28"
                        Padding="11,0"
                        Click="{x:Bind OnMaximizeClicked}"
                        FontSize="12"
                        IsEnabled="{x:Bind IsWindowMaximized.Equals(x:False), Mode=OneWay}"
                        Text="{x:Bind rootservice:ResourceService.GetLocalized('Installer/Maximize')}">

                        <MenuFlyoutItem.Icon>
                            <FontIcon Margin="6" Glyph="&#xE922;" />
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>

                    <MenuFlyoutSeparator Width="200" />

                    <MenuFlyoutItem
                        Height="28"
                        Padding="11,0"
                        Click="{x:Bind OnCloseClicked}"
                        FontSize="12"
                        Text="{x:Bind rootservice:ResourceService.GetLocalized('Installer/Close')}">

                        <MenuFlyoutItem.Icon>
                            <FontIcon Margin="6" Glyph="&#xE8BB;" />
                        </MenuFlyoutItem.Icon>

                        <MenuFlyoutItem.KeyboardAccelerators>
                            <KeyboardAccelerator Key="F4" Modifiers="Menu" />
                        </MenuFlyoutItem.KeyboardAccelerators>
                    </MenuFlyoutItem>
                </MenuFlyout>
            </ResourceDictionary>
        </Page.Resources>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!--  应用标题栏内容  -->
            <TitleBar
                x:Name="AppTitlebar"
                Title="{x:Bind rootservice:ResourceService.GetLocalized('WebView/Title')}"
                Height="45"
                Margin="16,0,0,0"
                VerticalAlignment="Top">

                <TitleBar.LeftHeader>
                    <ImageIcon
                        MaxWidth="16"
                        MaxHeight="16"
                        Margin="2,0,2,0"
                        Source="ms-appx:///Assets/Icon/Control/Web.png" />
                </TitleBar.LeftHeader>
            </TitleBar>

            <Grid
                Grid.Row="1"
                Height="45"
                Padding="0">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!--  左侧控制栏  -->
                <CommandBar
                    Grid.Column="0"
                    Margin="4,0,0,0"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    Background="Transparent"
                    DefaultLabelPosition="Right">

                    <CommandBar.PrimaryCommands>
                        <AppBarButton
                            Click="{x:Bind OnBrowserBackClicked}"
                            IsEnabled="{x:Bind CanGoBack, Mode=OneWay}"
                            Label="{x:Bind rootservice:ResourceService.GetLocalized('WebView/Back')}">

                            <AppBarButton.Icon>
                                <FontIcon Glyph="&#xE72B;" />
                            </AppBarButton.Icon>
                        </AppBarButton>

                        <AppBarButton
                            Click="{x:Bind OnBrowserForwardClicked}"
                            IsEnabled="{x:Bind CanGoForward, Mode=OneWay}"
                            Label="{x:Bind rootservice:ResourceService.GetLocalized('WebView/Forward')}">

                            <AppBarButton.Icon>
                                <FontIcon Glyph="&#xE72A;" />
                            </AppBarButton.Icon>
                        </AppBarButton>

                        <AppBarButton
                            Click="{x:Bind OnRefreshClicked}"
                            IsEnabled="{x:Bind IsLoading.Equals(x:False), Mode=OneWay}"
                            Label="{x:Bind rootservice:ResourceService.GetLocalized('WebView/Refresh')}">

                            <AppBarButton.Icon>
                                <FontIcon Glyph="&#xE72C;" />
                            </AppBarButton.Icon>
                        </AppBarButton>
                    </CommandBar.PrimaryCommands>
                </CommandBar>

                <!--  网页标题  -->
                <Grid
                    Grid.Column="1"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <ProgressRing
                        Grid.Column="0"
                        Width="20"
                        Height="20"
                        Margin="0,0,5,0"
                        HorizontalAlignment="Right"
                        IsActive="{x:Bind IsLoading, Mode=OneWay}"
                        Visibility="{x:Bind IsLoading, Mode=OneWay}" />
                    <TextBlock
                        Grid.Column="1"
                        VerticalAlignment="Center"
                        Text="{x:Bind rootservice:ResourceService.GetLocalized('WebView/WebTitle')}" />
                    <TextBlock
                        Grid.Column="2"
                        VerticalAlignment="Center"
                        Text="{x:Bind rootservice:ResourceService.GetLocalized('WebView/Loading')}"
                        Visibility="{x:Bind IsLoading, Mode=OneWay}" />
                </Grid>

                <!--  右侧控制栏  -->
                <CommandBar
                    Grid.Column="2"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"
                    Background="Transparent"
                    DefaultLabelPosition="Right">

                    <CommandBar.PrimaryCommands>
                        <AppBarButton
                            Click="{x:Bind OnDownloadClicked}"
                            IsEnabled="{x:Bind IsEnabled, Mode=OneWay}"
                            Label="{x:Bind rootservice:ResourceService.GetLocalized('WebView/Download')}">

                            <AppBarButton.Icon>
                                <FontIcon Glyph="&#xE896;" />
                            </AppBarButton.Icon>
                        </AppBarButton>
                    </CommandBar.PrimaryCommands>
                </CommandBar>

                <Button
                    Grid.Column="3"
                    MinHeight="{ThemeResource AppBarThemeCompactHeight}"
                    Margin="-6,0,0,0"
                    Padding="{ThemeResource CommandBarMoreButtonMargin}"
                    VerticalAlignment="Center"
                    Foreground="{ThemeResource CommandBarForeground}"
                    IsAccessKeyScope="True"
                    Style="{ThemeResource EllipsisButton}">

                    <Button.Content>
                        <FontIcon
                            Height="{ThemeResource AppBarExpandButtonCircleDiameter}"
                            VerticalAlignment="Center"
                            FontFamily="{ThemeResource SymbolThemeFontFamily}"
                            FontSize="20"
                            Glyph="&#xE712;" />
                    </Button.Content>

                    <Button.Flyout>
                        <MenuFlyout Placement="Bottom">
                            <MenuFlyoutItem Click="{x:Bind OnOpenWithBrowserClicked}" Text="{x:Bind rootservice:ResourceService.GetLocalized('WebView/OpenWithBrowser')}">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xE774;" />
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>

                            <MenuFlyoutSeparator />

                            <MenuFlyoutItem Click="{x:Bind OnOpenCacheFolderClicked}" Text="{x:Bind rootservice:ResourceService.GetLocalized('WebView/OpenCacheFolder')}">

                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xE8b7;" />
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>

                            <MenuFlyoutItem
                                Click="{x:Bind OnClearWebCacheClicked}"
                                IsEnabled="{x:Bind IsEnabled, Mode=OneWay}"
                                Text="{x:Bind rootservice:ResourceService.GetLocalized('WebView/ClearWebCache')}">

                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xE74D;" />
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>

                            <MenuFlyoutSeparator />

                            <MenuFlyoutItem Click="{x:Bind OnOpenSettingsClicked}" Text="{x:Bind rootservice:ResourceService.GetLocalized('WebView/Settings')}">

                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xE713;" />
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>

                            <MenuFlyoutSeparator />

                            <MenuFlyoutItem
                                Click="{x:Bind OnBrowserInformationClicked}"
                                IsEnabled="{x:Bind IsEnabled, Mode=OneWay}"
                                Text="{x:Bind rootservice:ResourceService.GetLocalized('WebView/BrowserInformation')}">

                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xE946;" />
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                        </MenuFlyout>
                    </Button.Flyout>

                    <ToolTipService.ToolTip>
                        <ToolTip
                            MaxWidth="9999"
                            Content="{x:Bind rootservice:ResourceService.GetLocalized('WebView/ViewMore')}"
                            Loaded="{x:Bind backdrophelper:ToolTipBackdropHelper.OnLoaded}" />
                    </ToolTipService.ToolTip>
                </Button>
            </Grid>

            <!--  网页控件  -->
            <WebView2
                x:Name="WebViewBrowser"
                Grid.Row="2"
                CoreProcessFailed="{x:Bind OnCoreProcessFailed}"
                CoreWebView2Initialized="{x:Bind OnCoreWebView2Initialized}"
                FlowDirection="LeftToRight"
                NavigationCompleted="{x:Bind OnWebView2NavigationCompleted}"
                NavigationStarting="{x:Bind OnWebView2NavigationStarting}" />

            <VisualStateManager.VisualStateGroups>
                <VisualStateGroup>
                    <VisualState x:Name="BackgroundDefault">
                        <VisualState.Setters>
                            <Setter Target="WebViewPage.Background" Value="{ThemeResource DefaultBackgroundColorBrush}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="BackgroundTransparent">
                        <VisualState.Setters>
                            <Setter Target="WebViewPage.Background" Value="Transparent" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>
        </Grid>
    </Page>
</Window>
