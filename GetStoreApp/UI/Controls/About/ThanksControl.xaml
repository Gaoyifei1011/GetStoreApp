<Expander
    x:Class="GetStoreApp.UI.Controls.About.ThanksControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converter="using:GetStoreApp.Helpers.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:model="using:GetStoreApp.Models.Base"
    xmlns:root="using:GetStoreApp.Services.Root"
    Style="{StaticResource ExpanderAboutStyle}"
    mc:Ignorable="d">

    <Expander.Header>
        <TextBlock
            Style="{StaticResource ExpanderHeaderStyle}"
            Text="{x:Bind root:ResourceService.GetLocalized('About/Thanks')}"
            TextWrapping="Wrap" />
    </Expander.Header>

    <Expander.Content>
        <StackPanel Margin="9,0,10,0">
            <TextBlock
                Margin="5,0,0,5"
                Style="{StaticResource ExpanderContentTitleStyle}"
                Text="{x:Bind root:ResourceService.GetLocalized('About/ThanksDescription')}" />

            <GridView
                Margin="5,0"
                Padding="5"
                HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Stretch"
                BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="{ThemeResource ControlCornerRadius}"
                ItemsSource="{x:Bind ThanksList}"
                SelectionMode="None">

                <GridView.ItemContainerStyle>
                    <Style TargetType="GridViewItem">
                        <Setter Property="Margin" Value="0" />
                        <Setter Property="MinHeight" Value="0" />
                    </Style>
                </GridView.ItemContainerStyle>

                <GridView.ItemTemplate>
                    <DataTemplate x:DataType="model:KeyValuePairModel">
                        <HyperlinkButton
                            Width="150"
                            Padding="5,8"
                            HorizontalContentAlignment="Center"
                            Content="{x:Bind Key}"
                            NavigateUri="{x:Bind Value}">

                            <ToolTipService.ToolTip>
                                <ToolTip Content="{x:Bind converter:StringConverterHelper.AboutThanksToolTipFormat(Key)}" Placement="Mouse" />
                            </ToolTipService.ToolTip>
                        </HyperlinkButton>
                    </DataTemplate>
                </GridView.ItemTemplate>
            </GridView>
        </StackPanel>
    </Expander.Content>
</Expander>
