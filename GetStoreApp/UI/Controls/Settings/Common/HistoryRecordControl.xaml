<Expander
    x:Class="GetStoreApp.UI.Controls.Settings.Common.HistoryRecordControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:root="using:GetStoreApp.Services.Root"
    Padding="0"
    HorizontalAlignment="Stretch"
    HorizontalContentAlignment="Stretch"
    IsExpanded="True"
    mc:Ignorable="d">

    <Expander.Header>
        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <FontIcon
                Grid.Column="0"
                Margin="0,0,12,0"
                FontFamily="{ThemeResource SymbolThemeFontFamily}"
                FontSize="16"
                Glyph="&#xF738;" />

            <StackPanel
                Grid.Column="1"
                Margin="0,12"
                VerticalAlignment="Center"
                Orientation="Vertical">

                <TextBlock Style="{StaticResource BodyTextBlockStyle}" Text="{x:Bind root:ResourceService.GetLocalized('Settings/HistoryRecord')}" />

                <TextBlock
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Style="{StaticResource CaptionTextBlockStyle}"
                    Text="{x:Bind root:ResourceService.GetLocalized('Settings/HistoryRecordDescription')}" />
            </StackPanel>
        </Grid>
    </Expander.Header>

    <Expander.Content>
        <StackPanel Padding="43,6,16,4" HorizontalAlignment="Stretch">
            <StackPanel>
                <Grid HorizontalAlignment="Stretch" VerticalAlignment="Center">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Vertical">
                        <TextBlock
                            VerticalAlignment="Center"
                            Style="{StaticResource BodyTextBlockStyle}"
                            Text="{x:Bind root:ResourceService.GetLocalized('Settings/HistoryLite')}" />

                        <TextBlock
                            VerticalAlignment="Center"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            Style="{StaticResource CaptionTextBlockStyle}"
                            Text="{x:Bind root:ResourceService.GetLocalized('Settings/HistoryLiteDescription')}" />
                    </StackPanel>

                    <DropDownButton
                        Grid.Column="1"
                        VerticalAlignment="Center"
                        Content="{x:Bind HistoryLiteItem.HistoryLiteNumName, Mode=OneWay}">

                        <ToolTipService.ToolTip>
                            <ToolTip Content="{x:Bind HistoryLiteItem.HistoryLiteNumName, Mode=OneWay}" Placement="Mouse" />
                        </ToolTipService.ToolTip>

                        <DropDownButton.Flyout>
                            <MenuFlyout MenuFlyoutPresenterStyle="{StaticResource MenuFlyoutPresenterStyle}" Placement="Bottom">
                                <ToggleMenuFlyoutItem
                                    Click="{x:Bind OnHistoryLiteSelectClicked}"
                                    IsChecked="{x:Bind IsHistoryLiteItemChecked(HistoryLiteItem.HistoryLiteNumValue, HistoryLiteNumList[0].HistoryLiteNumValue), Mode=OneWay}"
                                    Style="{StaticResource ToggleMenuFlyoutItemStyle}"
                                    Tag="0"
                                    Text="{x:Bind HistoryLiteNumList[0].HistoryLiteNumName}">

                                    <ToolTipService.ToolTip>
                                        <ToolTip Content="{x:Bind HistoryLiteNumList[0].HistoryLiteNumName}" Placement="Mouse" />
                                    </ToolTipService.ToolTip>
                                </ToggleMenuFlyoutItem>

                                <ToggleMenuFlyoutItem
                                    Click="{x:Bind OnHistoryLiteSelectClicked}"
                                    IsChecked="{x:Bind IsHistoryLiteItemChecked(HistoryLiteItem.HistoryLiteNumValue, HistoryLiteNumList[1].HistoryLiteNumValue), Mode=OneWay}"
                                    Style="{StaticResource ToggleMenuFlyoutItemStyle}"
                                    Tag="1"
                                    Text="{x:Bind HistoryLiteNumList[1].HistoryLiteNumName}">

                                    <ToolTipService.ToolTip>
                                        <ToolTip Content="{x:Bind HistoryLiteNumList[1].HistoryLiteNumName}" Placement="Mouse" />
                                    </ToolTipService.ToolTip>
                                </ToggleMenuFlyoutItem>
                            </MenuFlyout>
                        </DropDownButton.Flyout>
                    </DropDownButton>
                </Grid>
            </StackPanel>

            <MenuFlyoutSeparator Margin="0,4,0,4" />

            <StackPanel Margin="0,0,0,6">
                <Grid HorizontalAlignment="Stretch" VerticalAlignment="Center">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Vertical">
                        <TextBlock
                            VerticalAlignment="Center"
                            Style="{StaticResource BodyTextBlockStyle}"
                            Text="{x:Bind root:ResourceService.GetLocalized('Settings/HistoryJumpList')}" />

                        <TextBlock
                            VerticalAlignment="Center"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            Style="{StaticResource CaptionTextBlockStyle}"
                            Text="{x:Bind root:ResourceService.GetLocalized('Settings/HistoryJumpListDescription')}" />
                    </StackPanel>

                    <DropDownButton
                        Grid.Column="1"
                        VerticalAlignment="Center"
                        Content="{x:Bind HistoryJumpListItem.HistoryJumpListNumName, Mode=OneWay}">

                        <ToolTipService.ToolTip>
                            <ToolTip Content="{x:Bind HistoryJumpListItem.HistoryJumpListNumName, Mode=OneWay}" Placement="Mouse" />
                        </ToolTipService.ToolTip>

                        <DropDownButton.Flyout>
                            <MenuFlyout MenuFlyoutPresenterStyle="{StaticResource MenuFlyoutPresenterStyle}" Placement="Bottom">
                                <ToggleMenuFlyoutItem
                                    Click="{x:Bind OnHistoryJumpListSelectClicked}"
                                    IsChecked="{x:Bind IsHistoryJumpListItemChecked(HistoryJumpListItem.HistoryJumpListNumValue, HistoryJumpListNumList[0].HistoryJumpListNumValue), Mode=OneWay}"
                                    Style="{StaticResource ToggleMenuFlyoutItemStyle}"
                                    Tag="0"
                                    Text="{x:Bind HistoryJumpListNumList[0].HistoryJumpListNumName}">

                                    <ToolTipService.ToolTip>
                                        <ToolTip Content="{x:Bind HistoryJumpListNumList[0].HistoryJumpListNumName}" Placement="Mouse" />
                                    </ToolTipService.ToolTip>
                                </ToggleMenuFlyoutItem>

                                <ToggleMenuFlyoutItem
                                    Click="{x:Bind OnHistoryJumpListSelectClicked}"
                                    IsChecked="{x:Bind IsHistoryJumpListItemChecked(HistoryJumpListItem.HistoryJumpListNumValue, HistoryJumpListNumList[1].HistoryJumpListNumValue), Mode=OneWay}"
                                    Style="{StaticResource ToggleMenuFlyoutItemStyle}"
                                    Tag="1"
                                    Text="{x:Bind HistoryJumpListNumList[1].HistoryJumpListNumName}">

                                    <ToolTipService.ToolTip>
                                        <ToolTip Content="{x:Bind HistoryJumpListNumList[1].HistoryJumpListNumName}" Placement="Mouse" />
                                    </ToolTipService.ToolTip>
                                </ToggleMenuFlyoutItem>

                                <ToggleMenuFlyoutItem
                                    Click="{x:Bind OnHistoryJumpListSelectClicked}"
                                    IsChecked="{x:Bind IsHistoryJumpListItemChecked(HistoryJumpListItem.HistoryJumpListNumValue, HistoryJumpListNumList[2].HistoryJumpListNumValue), Mode=OneWay}"
                                    Style="{StaticResource ToggleMenuFlyoutItemStyle}"
                                    Tag="2"
                                    Text="{x:Bind HistoryJumpListNumList[2].HistoryJumpListNumName}">

                                    <ToolTipService.ToolTip>
                                        <ToolTip Content="{x:Bind HistoryJumpListNumList[2].HistoryJumpListNumName}" Placement="Mouse" />
                                    </ToolTipService.ToolTip>
                                </ToggleMenuFlyoutItem>

                                <ToggleMenuFlyoutItem
                                    Click="{x:Bind OnHistoryJumpListSelectClicked}"
                                    IsChecked="{x:Bind IsHistoryJumpListItemChecked(HistoryJumpListItem.HistoryJumpListNumValue, HistoryJumpListNumList[3].HistoryJumpListNumValue), Mode=OneWay}"
                                    Style="{StaticResource ToggleMenuFlyoutItemStyle}"
                                    Tag="3"
                                    Text="{x:Bind HistoryJumpListNumList[3].HistoryJumpListNumName}">

                                    <ToolTipService.ToolTip>
                                        <ToolTip Content="{x:Bind HistoryJumpListNumList[3].HistoryJumpListNumName}" Placement="Mouse" />
                                    </ToolTipService.ToolTip>
                                </ToggleMenuFlyoutItem>

                                <ToggleMenuFlyoutItem
                                    Click="{x:Bind OnHistoryJumpListSelectClicked}"
                                    IsChecked="{x:Bind IsHistoryJumpListItemChecked(HistoryJumpListItem.HistoryJumpListNumValue, HistoryJumpListNumList[4].HistoryJumpListNumValue), Mode=OneWay}"
                                    Style="{StaticResource ToggleMenuFlyoutItemStyle}"
                                    Tag="4"
                                    Text="{x:Bind HistoryJumpListNumList[4].HistoryJumpListNumName}">

                                    <ToolTipService.ToolTip>
                                        <ToolTip Content="{x:Bind HistoryJumpListNumList[4].HistoryJumpListNumName}" Placement="Mouse" />
                                    </ToolTipService.ToolTip>
                                </ToggleMenuFlyoutItem>
                            </MenuFlyout>
                        </DropDownButton.Flyout>
                    </DropDownButton>
                </Grid>
            </StackPanel>
        </StackPanel>
    </Expander.Content>
</Expander>
