<UserControl
    x:Class="GetStoreApp.UI.Controls.Settings.Appearance.AlwaysShowBackdropControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="using:Microsoft.Xaml.Interactivity"
    xmlns:ic="using:Microsoft.Xaml.Interactions.Core"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Style="{StaticResource CardGridStyle}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <FontIcon
            Grid.Column="0"
            FontFamily="{StaticResource SegoeFluentFontFamily}"
            Glyph="&#xE81E;"
            Style="{StaticResource FontIconStyle}" />

        <StackPanel Grid.Column="1" Orientation="Vertical">

            <TextBlock
                VerticalAlignment="Center"
                Style="{StaticResource BodyTextBlockStyle}"
                Text="{x:Bind ResourceService.GetLocalized('/Settings/AlwaysShowBackdrop')}" />

            <TextBlock
                VerticalAlignment="Center"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Style="{StaticResource CaptionTextBlockStyle}"
                Text="{x:Bind ResourceService.GetLocalized('/Settings/AlwaysShowBackdropDescription')}" />
        </StackPanel>

        <ToggleSwitch
            Name="AlwaysShowBackdropToggleSwitch"
            Grid.Column="2"
            FlowDirection="RightToLeft"
            IsOn="{x:Bind ViewModel.AlwaysShowBackdropValue, Mode=OneWay}"
            OffContent="{x:Bind ResourceService.GetLocalized('/Settings/AlwaysShowBackdropOffContent')}"
            OnContent="{x:Bind ResourceService.GetLocalized('/Settings/AlwaysShowBackdropOnContent')}">

            <i:Interaction.Behaviors>
                <ic:EventTriggerBehavior EventName="Toggled">
                    <ic:InvokeCommandAction Command="{x:Bind ViewModel.AlwaysShowBackdropCommand}" CommandParameter="{Binding ElementName=AlwaysShowBackdropToggleSwitch, Path=IsOn}" />
                </ic:EventTriggerBehavior>
            </i:Interaction.Behaviors>
        </ToggleSwitch>
    </Grid>
</UserControl>
