<Grid
    x:Class="GetStoreApp.UI.Controls.Settings.Advanced.InstallModeControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controlExtensions="using:GetStoreApp.Helpers.Controls.Extensions"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:menusAndToolbars="using:GetStoreApp.Views.CustomControls.MenusAndToolbars"
    xmlns:root="using:GetStoreApp.Services.Root"
    xmlns:settingsViewModel="using:GetStoreApp.ViewModels.Controls.Settings.Advanced"
    Style="{StaticResource CardGridStyle}"
    mc:Ignorable="d">

    <Grid.DataContext>
        <settingsViewModel:InstallModeViewModel x:Name="ViewModel" />
    </Grid.DataContext>

    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition Width="Auto" />
    </Grid.ColumnDefinitions>

    <FontIcon
        Grid.Column="0"
        FontFamily="{StaticResource SettingsFluentFontFamily}"
        Glyph="&#xF775;"
        Style="{StaticResource FontIconStyle}" />

    <StackPanel Grid.Column="1" Orientation="Vertical">

        <TextBlock
            VerticalAlignment="Center"
            Style="{StaticResource BodyTextBlockStyle}"
            Text="{x:Bind root:ResourceService.GetLocalized('Settings/InstallMode')}" />

        <TextBlock
            VerticalAlignment="Center"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
            Style="{StaticResource CaptionTextBlockStyle}"
            Text="{x:Bind root:ResourceService.GetLocalized('Settings/InstallModeDescription')}" />
    </StackPanel>

    <Button
        Grid.Column="2"
        VerticalAlignment="Center"
        controlExtensions:CursorHelper.Cursor="Hand"
        Click="{x:Bind ViewModel.OnInstallModeTipClicked}"
        Style="{StaticResource ButtonInfoStyle}">

        <Button.Content>
            <FontIcon
                FontSize="16"
                Glyph="&#xE946;"
                Style="{StaticResource SegoeFluentStyle}" />
        </Button.Content>

        <ToolTipService.ToolTip>
            <ToolTip
                Content="{x:Bind root:ResourceService.GetLocalized('Settings/InstallModeToolTip')}"
                Placement="Bottom"
                VerticalOffset="25" />
        </ToolTipService.ToolTip>
    </Button>

    <DropDownButton
        Grid.Column="3"
        VerticalAlignment="Center"
        Content="{x:Bind ViewModel.InstallMode.DisplayName, Mode=OneWay}">

        <ToolTipService.ToolTip>
            <ToolTip
                Content="{x:Bind ViewModel.InstallMode.DisplayName, Mode=OneWay}"
                Placement="Bottom"
                VerticalOffset="20" />
        </ToolTipService.ToolTip>

        <DropDownButton.Flyout>
            <menusAndToolbars:AdaptiveMenuFlyout>
                <RadioMenuFlyoutItem
                    Click="{x:Bind ViewModel.OnInstallModeSelectClicked}"
                    GroupName="InstallModeSetting"
                    IsChecked="{x:Bind IsItemChecked(ViewModel.InstallMode.InternalName, ViewModel.InstallModeList[0].InternalName), Mode=OneWay}"
                    Tag="0"
                    Text="{x:Bind ViewModel.InstallModeList[0].DisplayName}">

                    <ToolTipService.ToolTip>
                        <ToolTip
                            Content="{x:Bind ViewModel.InstallModeList[0].DisplayName}"
                            Placement="Bottom"
                            VerticalOffset="20" />
                    </ToolTipService.ToolTip>
                </RadioMenuFlyoutItem>

                <RadioMenuFlyoutItem
                    Click="{x:Bind ViewModel.OnInstallModeSelectClicked}"
                    GroupName="InstallModeSetting"
                    IsChecked="{x:Bind IsItemChecked(ViewModel.InstallMode.InternalName, ViewModel.InstallModeList[1].InternalName), Mode=OneWay}"
                    Tag="1"
                    Text="{x:Bind ViewModel.InstallModeList[1].DisplayName}">

                    <ToolTipService.ToolTip>
                        <ToolTip
                            Content="{x:Bind ViewModel.InstallModeList[1].DisplayName}"
                            Placement="Bottom"
                            VerticalOffset="20" />
                    </ToolTipService.ToolTip>
                </RadioMenuFlyoutItem>
            </menusAndToolbars:AdaptiveMenuFlyout>
        </DropDownButton.Flyout>
    </DropDownButton>
</Grid>
