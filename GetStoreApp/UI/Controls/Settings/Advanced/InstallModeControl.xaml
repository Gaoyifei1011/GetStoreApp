<UserControl
    x:Class="GetStoreApp.UI.Controls.Settings.Advanced.InstallModeControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:root="using:GetStoreApp.Services.Root"
    xmlns:settingsViewModel="using:GetStoreApp.ViewModels.Controls.Settings.Advanced"
    mc:Ignorable="d">

    <UserControl.DataContext>
        <settingsViewModel:InstallModeViewModel x:Name="ViewModel" />
    </UserControl.DataContext>

    <Grid VerticalAlignment="Center" Style="{StaticResource CardGridStyle}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <FontIcon
            Grid.Column="0"
            FontFamily="{StaticResource SettingsFluentFontFamily}"
            Glyph="&#xF775;"
            Style="{StaticResource FontIconStyle}" />

        <StackPanel Grid.Column="1" Orientation="Vertical">

            <TextBlock
                VerticalAlignment="Center"
                Style="{StaticResource BodyTextBlockStyle}"
                Text="{x:Bind root:ResourceService.GetLocalized('/Settings/InstallMode')}" />

            <TextBlock
                VerticalAlignment="Center"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Style="{StaticResource CaptionTextBlockStyle}"
                Text="{x:Bind root:ResourceService.GetLocalized('/Settings/InstallModeDescription')}" />
        </StackPanel>

        <Button
            Grid.Column="2"
            VerticalAlignment="Center"
            Command="{x:Bind ViewModel.InstallModeTipCommand}"
            Style="{StaticResource ButtonInfoStyle}"
            ToolTipService.ToolTip="{x:Bind root:ResourceService.GetLocalized('/Settings/InstallModeToolTip')}">
            <Button.Content>
                <FontIcon
                    FontSize="16"
                    Glyph="&#xE946;"
                    Style="{StaticResource SegoeFluentStyle}" />
            </Button.Content>
        </Button>

        <DropDownButton
            Grid.Column="3"
            VerticalAlignment="Center"
            Content="{x:Bind ViewModel.InstallMode.DisplayName, Mode=OneWay}"
            ToolTipService.ToolTip="{x:Bind ViewModel.InstallMode.DisplayName, Mode=OneWay}">
            <DropDownButton.Flyout>
                <MenuFlyout Placement="Bottom">
                    <RadioMenuFlyoutItem
                        Command="{x:Bind ViewModel.InstallModeSelectCommand}"
                        CommandParameter="0"
                        GroupName="InstallModeSetting"
                        IsChecked="{x:Bind IsItemChecked(ViewModel.InstallMode.InternalName, ViewModel.InstallModeList[0].InternalName), Mode=OneWay}"
                        Text="{x:Bind ViewModel.InstallModeList[0].DisplayName}"
                        ToolTipService.ToolTip="{x:Bind ViewModel.InstallModeList[0].DisplayName}" />
                    <RadioMenuFlyoutItem
                        Command="{x:Bind ViewModel.InstallModeSelectCommand}"
                        CommandParameter="1"
                        GroupName="InstallModeSetting"
                        IsChecked="{x:Bind IsItemChecked(ViewModel.InstallMode.InternalName, ViewModel.InstallModeList[1].InternalName), Mode=OneWay}"
                        Text="{x:Bind ViewModel.InstallModeList[1].DisplayName}"
                        ToolTipService.ToolTip="{x:Bind ViewModel.InstallModeList[1].DisplayName}" />
                </MenuFlyout>
            </DropDownButton.Flyout>
        </DropDownButton>
    </Grid>
</UserControl>
