<UserControl
    x:Class="GetStoreApp.UI.Controls.Web.LoadFailedControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid HorizontalAlignment="Center">

        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="0.8*" />
            <RowDefinition Height="1.2*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" VerticalAlignment="Bottom">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock
                Grid.Row="0"
                Margin="0,0,0,5"
                HorizontalAlignment="Center"
                FontSize="20"
                FontWeight="SemiBold"
                Text="{x:Bind ResourceService.GetLocalized('/Web/LoadFailedTitle')}" />
            <TextBlock
                Grid.Row="1"
                Margin="0,0,0,30"
                HorizontalAlignment="Center"
                FontSize="16"
                Text="{x:Bind ResourceService.GetLocalized('/Web/LoadFailedDescription')}" />
        </Grid>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock
                Grid.Row="0"
                Margin="0,0,0,8"
                HorizontalAlignment="Left"
                FontSize="16"
                Text="{x:Bind ResourceService.GetLocalized('/Web/ProblemSolution')}" />
            <TextBlock
                Grid.Row="1"
                Margin="30,0,0,5"
                HorizontalAlignment="Center"
                Text="{x:Bind ResourceService.GetLocalized('/Web/Solution1')}" />
            <TextBlock
                Grid.Row="2"
                Margin="30,0,0,0"
                HorizontalAlignment="Left"
                Text="{x:Bind ResourceService.GetLocalized('/Web/Solution2')}" />
        </Grid>

        <Grid
            Grid.Row="2"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Top"
            ColumnSpacing="30">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button
                Grid.Column="0"
                HorizontalAlignment="Right"
                Command="{x:Bind ViewModel.DownloadWebView2Command}"
                Content="{x:Bind ResourceService.GetLocalized('/Web/DownloadWebView2')}"
                Style="{StaticResource AccentButtonStyle}"
                ToolTipService.ToolTip="{x:Bind ResourceService.GetLocalized('/Web/DownloadWebView2')}" />
            <Button
                Grid.Column="1"
                HorizontalAlignment="Left"
                Command="{x:Bind ViewModel.OpenWithBrowserCommand}"
                Content="{x:Bind ResourceService.GetLocalized('/Web/OpenWithBrowser')}"
                ToolTipService.ToolTip="{x:Bind ResourceService.GetLocalized('/Web/OpenWithBrowser')}" />
        </Grid>
    </Grid>
</UserControl>
