<ContentDialog
    x:Class="GetStoreApp.UI.Dialogs.DownloadNotifyDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:behavior="using:GetStoreApp.Behaviors"
    xmlns:i="using:Microsoft.Xaml.Interactivity"
    Title="{x:Bind ResourceService.GetLocalized('/Dialog/DownloadNotifyTitle')}"
    BorderBrush="{ThemeResource SystemAccentColor}"
    CloseButtonText="{x:Bind ResourceService.GetLocalized('/Dialog/CancelDownload')}"
    Content="{x:Bind DownloadNotifyContent}"
    PrimaryButtonStyle="{ThemeResource AccentButtonStyle}"
    PrimaryButtonText="{x:Bind ResourceService.GetLocalized('/Dialog/Redownload')}"
    RequestedTheme="{x:Bind DialogTheme}"
    SecondaryButtonText="{x:Bind ResourceService.GetLocalized('/Dialog/ViewDownloadRecord')}"
    Style="{StaticResource DefaultContentDialogStyle}"
    mc:Ignorable="d">

    <i:Interaction.Behaviors>
        <behavior:ContentDialogBehavior />
    </i:Interaction.Behaviors>
</ContentDialog>
