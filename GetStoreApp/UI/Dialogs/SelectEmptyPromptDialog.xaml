<ContentDialog
    x:Class="GetStoreApp.UI.Dialogs.SelectEmptyPromptDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:behavior="using:GetStoreApp.Behaviors"
    xmlns:i="using:Microsoft.Xaml.Interactivity"
    Title="{x:Bind ResourceService.GetLocalized('/Dialog/SelectEmptyTitle')}"
    BorderBrush="{ThemeResource SystemAccentColor}"
    CloseButtonText="{x:Bind ResourceService.GetLocalized('/Dialog/OK')}"
    Content="{x:Bind ResourceService.GetLocalized('/Dialog/SelectEmptyContent')}"
    RequestedTheme="{x:Bind DialogTheme}"
    Style="{StaticResource DefaultContentDialogStyle}"
    mc:Ignorable="d">

    <i:Interaction.Behaviors>
        <behavior:ContentDialogBehavior />
    </i:Interaction.Behaviors>
</ContentDialog>
