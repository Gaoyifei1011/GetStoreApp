<Page
    x:Class="GetStoreApp.Views.Pages.ShellPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:helpers="using:GetStoreApp.Helpers.Shell"
    xmlns:i="using:Microsoft.Xaml.Interactivity"
    xmlns:ic="using:Microsoft.Xaml.Interactions.Core"
    xmlns:sc="using:GetStoreApp.UI.Controls.Shell"
    xmlns:shell="using:GetStoreApp.Views.Shell"
    Unloaded="{x:Bind ShellPageUnloaded}">

    <i:Interaction.Behaviors>
        <ic:EventTriggerBehavior EventName="Loaded">
            <ic:InvokeCommandAction Command="{x:Bind ViewModel.LoadedCommand}" />
        </ic:EventTriggerBehavior>
        <ic:EventTriggerBehavior EventName="Unloaded">
            <ic:InvokeCommandAction Command="{x:Bind ViewModel.UnloadedCommand}" />
        </ic:EventTriggerBehavior>
        <ic:EventTriggerBehavior EventName="SizeChanged">
            <ic:InvokeCommandAction Command="{x:Bind ViewModel.SizeChangedCommand}" />
        </ic:EventTriggerBehavior>
    </i:Interaction.Behaviors>

    <Grid>
        <sc:AppTitleBarControl
            x:Name="AppTitleBar"
            Height="45"
            Margin="{x:Bind NavigationViewControl.PaneDisplayMode, Converter={StaticResource AppTitleBarMarginConverter}, Mode=OneWay}"
            VerticalAlignment="Top" />

        <NavigationView
            x:Name="NavigationViewControl"
            IsBackEnabled="{x:Bind ViewModel.IsBackEnabled, Mode=OneWay}"
            IsPaneToggleButtonVisible="{x:Bind ViewModel.PaneToggleButtonVisible, Mode=OneWay}"
            IsTitleBarAutoPaddingEnabled="True"
            OpenPaneLength="220"
            PaneDisplayMode="{x:Bind ViewModel.NavigationDispalyMode, Mode=OneWay}"
            SelectedItem="{x:Bind ViewModel.SelectedItem, Mode=OneWay}">

            <NavigationView.MenuItems>
                <NavigationViewItemHeader Content="{x:Bind ResourceService.GetLocalized('/Shell/Title')}" Foreground="{ThemeResource NavigationViewItemForeground}" />

                <NavigationViewItem helpers:NavigationHelper.NavigateTo="GetStoreApp.ViewModels.Pages.HomeViewModel" Content="{x:Bind ResourceService.GetLocalized('/Shell/Home')}">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE80f;" Style="{StaticResource SegoeFluentStyle}" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItem helpers:NavigationHelper.NavigateTo="GetStoreApp.ViewModels.Pages.HistoryViewModel" Content="{x:Bind ResourceService.GetLocalized('/Shell/History')}">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xF738;" Style="{StaticResource SegoeFluentStyle}" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItem helpers:NavigationHelper.NavigateTo="GetStoreApp.ViewModels.Pages.DownloadViewModel" Content="{x:Bind ResourceService.GetLocalized('/Shell/Download')}">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE896;" Style="{StaticResource SegoeFluentStyle}" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItem helpers:NavigationHelper.NavigateTo="GetStoreApp.ViewModels.Pages.WebViewModel" Content="{x:Bind ResourceService.GetLocalized('/Shell/Web')}">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE774;" Style="{StaticResource SegoeFluentStyle}" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <NavigationView.FooterMenuItems>
                <NavigationViewItem helpers:NavigationHelper.NavigateTo="GetStoreApp.ViewModels.Pages.AboutViewModel" Content="{x:Bind ResourceService.GetLocalized('/Shell/About')}">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE946;" Style="{StaticResource SegoeFluentStyle}" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.FooterMenuItems>

            <Frame x:Name="NavigationFrame" />
        </NavigationView>

        <controls:InAppNotification x:Name="ShellNotification" Style="{StaticResource InAppNotificationsStyle}" />
        <shell:TaskBarControl />
    </Grid>
</Page>
