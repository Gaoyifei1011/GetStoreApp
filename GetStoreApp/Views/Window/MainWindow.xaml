<WinUI:WASDKWindow
    x:Class="GetStoreApp.Views.Window.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:WinUI="using:GetStoreApp.Views.Window"
    xmlns:animatedvisuals="using:Microsoft.UI.Xaml.Controls.AnimatedVisuals"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:root="using:GetStoreApp.Services.Root"
    xmlns:wc="using:GetStoreApp.UI.Controls.Window"
    xmlns:window="using:GetStoreApp.Views.Window"
    xmlns:windowViewModel="using:GetStoreApp.ViewModels.Window"
    Title="{x:Bind root:ResourceService.GetLocalized('AppDisplayName')}"
    MinWidth="600"
    MinHeight="768"
    Activated="{x:Bind ViewModel.OnWindowActivated}"
    Closed="{x:Bind ViewModel.OnWindowClosed}"
    SizeChanged="{x:Bind ViewModel.OnWindowSizeChanged}"
    mc:Ignorable="d">

    <WinUI:WASDKWindow.DataContext>
        <windowViewModel:MainWindowViewModel x:Name="ViewModel" />
    </WinUI:WASDKWindow.DataContext>

    <Grid Background="{x:Bind ViewModel.AppBackground, Mode=OneWay}">

        <wc:AppTitleBarControl
            Height="45"
            Margin="{x:Bind ViewModel.AppTitleBarMargin, Mode=OneWay}"
            VerticalAlignment="Top" />

        <NavigationView
            BackRequested="{x:Bind ViewModel.OnNavigationViewBackRequested}"
            IsBackEnabled="{x:Bind ViewModel.IsBackEnabled, Mode=OneWay}"
            IsPaneToggleButtonVisible="{x:Bind ViewModel.IsPaneToggleButtonVisible, Mode=OneWay}"
            IsSettingsVisible="False"
            IsTitleBarAutoPaddingEnabled="True"
            ItemInvoked="{x:Bind ViewModel.OnNavigationViewItemInvoked}"
            Loaded="{x:Bind ViewModel.OnNavigationViewLoaded}"
            OpenPaneLength="220"
            PaneDisplayMode="{x:Bind ViewModel.NavigationDispalyMode, Mode=OneWay}"
            SelectedItem="{x:Bind ViewModel.SelectedItem, Mode=OneWay}"
            Unloaded="{x:Bind ViewModel.OnNavigationViewUnLoaded}">

            <NavigationView.MenuItems>
                <NavigationViewItemHeader Content="{x:Bind root:ResourceService.GetLocalized('/Window/Title')}" Foreground="{ThemeResource NavigationViewItemForeground}" />

                <NavigationViewItem Content="{x:Bind root:ResourceService.GetLocalized('/Window/Home')}" Tag="Home">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE80f;" Style="{StaticResource SegoeFluentStyle}" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItem Content="{x:Bind root:ResourceService.GetLocalized('/Window/History')}" Tag="History">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xF738;" Style="{StaticResource SegoeFluentStyle}" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItem Content="{x:Bind root:ResourceService.GetLocalized('/Window/Download')}" Tag="Download">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE896;" Style="{StaticResource SegoeFluentStyle}" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItem Content="{x:Bind root:ResourceService.GetLocalized('/Window/Web')}" Tag="Web">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE774;" Style="{StaticResource SegoeFluentStyle}" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <NavigationView.FooterMenuItems>
                <NavigationViewItem Content="{x:Bind root:ResourceService.GetLocalized('/Window/About')}" Tag="About">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE946;" Style="{StaticResource SegoeFluentStyle}" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="{x:Bind root:ResourceService.GetLocalized('/Window/Settings')}" Tag="Settings">
                    <NavigationViewItem.Icon>
                        <AnimatedIcon>
                            <AnimatedIcon.Source>
                                <animatedvisuals:AnimatedSettingsVisualSource />
                            </AnimatedIcon.Source>
                            <AnimatedIcon.FallbackIconSource>
                                <FontIconSource FontFamily="{StaticResource SegoeFluentFontFamily}" Glyph="&#xE713;" />
                            </AnimatedIcon.FallbackIconSource>
                        </AnimatedIcon>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.FooterMenuItems>

            <Frame
                x:Name="WindowFrame"
                Navigated="{x:Bind ViewModel.OnFrameNavigated}"
                NavigationFailed="{x:Bind ViewModel.OnFrameNavgationFailed}" />
        </NavigationView>

        <window:TaskBarControl />
    </Grid>
</WinUI:WASDKWindow>
