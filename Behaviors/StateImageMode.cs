namespace GetStoreApp.Behaviors
{
    /// <summary>
    /// StatusBar状态栏图标文件对应的枚举值
    /// The statusbar status bar icon file corresponds to the enumeration value
    /// </summary>
    public enum StateImageMode
    {
        Error = 0,
        Warning = 1,
        Notification = 2,
        Success = 3
    }
}
