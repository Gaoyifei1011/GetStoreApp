namespace GetStoreApp.Behaviors
{
    /// <summary>
    /// 主页面状态栏显示运行状态的Enum值
    /// The main page status bar displays the Enum value for the health status
    /// </summary>
    public enum StatImageMode
    {
        Error = 0,
        Warning = 1,
        Notification = 2,
        Success = 3
    }
}
